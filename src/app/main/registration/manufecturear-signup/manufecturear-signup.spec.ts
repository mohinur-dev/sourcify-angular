import { ManufecturearSignup } from './manufecturear-signup';

describe('ManufecturearSignup', () => {
  it('should create an instance', () => {
    expect(new ManufecturearSignup()).toBeTruthy();
  });
});
